# 从事件记忆到物理世界状态：论机器人世界模型缺失的变量

**张杰 · 独立研究者 · 2026年7月**


> 本文是英文 position paper《From Event Memory to Physical World State: A Position on the Missing Variable in Robot World Models》的中文全译版，与英文版同仓库发布。分析针对架构范式而非任何具体公司。

---

## 摘要

世界动作模型（World Action Models, WAM）已成为长时程机器人操作的新兴范式：将交互历史压缩为事件记忆，用于预测下一个子任务。本文指出，该范式建立在一个缺失的架构变量之上：**一个持久的、可修正的、受物理约束的世界状态**。通过剖析事件记忆类 WAM 的前沿系统，我们识别出四种系统性失效模式——**封存错误（sealed errors）、显著性遗忘（salience-driven forgetting）、重复推导（repeated re-derivation）、误差固化（error entrenchment）**——其根源在于：状态并非一个被持续维护的独立变量，而是每一步从只增不改的记忆中临时重建出来的。我们进一步论证，常被引用的三条状态性质（持久性、可观测修正性、物理一致性）必要但不充分；第四条性质——**反事实可模拟性（counterfactual simulability）**——既本质又代价最高。由此我们重新定位世界模型本身：世界模型不应充当机器人的状态表示，而应在一个独立维护的状态之上建模**状态转移核** $P(s'|s,a)$，当前状态 $s$ 的值则由单独的状态追踪机制持有。我们将这一解耦架构称为**物理世界状态（Physical World State, PWS）框架**，并展示它如何澄清具身智能中多个悬而未决的问题，包括任务执行中的目标变更、安全关键场景的仿真到现实迁移、以及当前系统为何在结构化工业环境中表现出色、却在家庭等非结构化环境中脆弱。文末给出研究议程与产业讨论，指出 PWS 的缺席——而非榜单分数——才是具身智能下一阶段部署的真正约束。

**关键词**：世界模型；具身智能；机器人操作；状态估计；长时程规划；世界动作模型；立场论文

---

## 一、引言：桌上的杯子

机器人把杯子放在桌上，转身去开门。

在人类观察者看来，这个瞬间什么都没"发生"——没有新动作，没有新事件。但在物理世界里，一切都变了：杯子从**被握持**变成**被支撑**，机器人的手从**占用**变成**空闲**。正是这两个变化合起来，才让"开门"成为可能。

这个平凡的瞬间，暴露了当代机器人世界模型核心的一个缺口。这个领域在压缩长交互历史上取得了惊人进展——延长上下文窗口、将事件蒸馏为记忆 token、检索相关片段——但基本回避了一个更根本的问题：**机器人对"世界当前状态"的持久表示是什么，又由谁来持有它？**

世界动作模型（WAM）的最新工作代表了压缩路线的前沿。这些系统不再延长上下文，而是把历史压缩为紧凑的事件记忆，用以预测下一个原子子任务。在标准基准上成绩亮眼，这一范式也正吸引大量产业投资——尤其是一类**世界模型原生（world-model-native）**公司，它们把世界模型本身定位为机器人的预训练基础模型，只需极少量上下文数据即可绑定到特定本体。

本文是一篇立场论文，不是综述，也不是基准报告。我们的主张很尖锐：

> **核心主张**
>
> 长时程具身智能的真正约束，不是记忆容量、检索质量，甚至不是世界模型的保真度，而是缺少一个独立维护的**物理世界状态（PWS）**：一个持久的、可修正的、受物理约束的、可反事实模拟的变量，它表示"**现在什么是真的**"，区别于"**发生过什么**"的记忆。事件记忆类 WAM 每一步都从只增不改的日志中重新推导状态，由此产生四种系统性失效模式——而这些失效模式恰恰在非结构化的安全关键环境（家庭、医院、高危场所）中是致命的，那里正是具身智能许诺最大价值的地方。

我们的论证分四步展开：第二节对事件记忆 WAM 范式做公允的解剖，指出它压缩了什么、丢失了什么；第三节将四种失效模式呈现为"状态即重算"的结构性后果；第四节引入 PWS 框架，包括我们提出的第四性质（反事实可模拟性）以及将世界模型重新定位为状态转移核而非状态表示；第五节讨论产业含义，包括为什么当前商业部署在结构化环境中成功，以及 PWS 视角对行业近期演化的预测。

**范围与立场。** 我们的批判是架构性的，不针对公司。我们分析的范式以近期前沿系统为代表——包括 WorldScape 系列工作及相关的物体可寻址方法——但论证适用于任何"状态从只增不改的记忆中重算、而非作为独立变量维护"的系统。涉及产业部署处，我们对具体公司做匿名化处理，使焦点保持在范式而非产品。我们也与前沿 WAM 系统的作者直接交流过；在相关处，我们注明哪些问题的回答仍停留在工程层面。

---

## 二、事件记忆范式解剖

要公允地批评一个范式，必须先准确说出它成就了什么、为何是真正的进步。

### 2.1 问题：任务各阶段在视觉上自相似

长时程机器人操作有一个广为人知的痼疾：**任务的不同阶段看起来可能一模一样**。子目标完成前和完成后的桌面，差异可能小到当前感知无法分辨——但正确的下一步动作完全取决于你已经完成了什么。延长上下文窗口并不解决这个问题，它只是推迟了"模型必须决定关注什么"的那个时刻。

### 2.2 WAM 的方案：历史 → 任务进度

世界动作模型范式的应对，是把压缩作为明确的架构目标：交互历史被蒸馏为 gist token；用余弦距离变化检测事件边界；保留显著的锚点；将全局历史视图与任务指令融合；检索结果用于条件化视觉-语言模型对下一个子任务的预测。

> **范式做对的地方**
>
> 这是第一次，**压缩历史**——而非**延长上下文**——被明确作为架构目标。"压缩后的任务进度表示能胜过原始长上下文方法"这一证明是真实贡献，工程实现也是扎实的。

但关键问题不是历史是否被压缩了，而是**压缩产生的是什么**。我们认为，它产生的是一个进度**估计**，而不是一个进度**状态**——这个差别是架构性的，不是表面文章。

### 2.3 两个结构性观察

**观察一："任务进度"是历史的无序平均。** 在我们考察的前沿方案中，全局历史分量是对全部历史 token 做均值池化（mean pooling）后与任务指令融合。均值池化抹掉顺序："先放杯子再开门"和"先开门再放杯子"，在平均之后几乎不可区分。也就是说，"做到哪一步了"这个核心概念，没有一个真正的状态变量在承载它，而是每一步都从一份无序摘要中**重新猜出来**的。

**观察二：所谓"自主规划"，是逐秒的预测。** 规划 prompt 是一个固定模板：给定任务指令和当前观测，预测**下一秒**的原子子任务。所谓长程规划，实际是"短程预测 × 记忆串联"的迭代——系统的有效视野是一秒，长程能力全部押在记忆检索的可靠性上。

> **结构性结论**
>
> 在这类系统里，**状态不是一个被维护的变量，而是一个被重算的产物**：每一步都从只增不改的记忆中重建。记忆是系统对世界的唯一表示，而记忆不是状态。

这是症结所在。本文余下部分将探讨：为什么这个机制是脆弱的，什么能替代它，以及为什么替代品比看上去更难。

---

## 三、四种系统性失效模式

如果状态每一步都从只增不改的记忆中重算，那么四种失效模式就会以结构性的必然随之而来。我们给它们命名，以便于讨论。

### 失效模式一：封存错误（Sealed errors）

每条历史记录在编码那一刻就被冻结。如果那一刻编码错了——遮挡、光照、视角，或单纯的感知错误——这条错误记录会**永远留在记忆里**，参与此后每一次检索。当后来的观测揭示"杯子其实并没有放稳"时，系统无法修订旧记录，只能追加一条新记录，让两条相互矛盾的记录竞争注意力。

真正的状态追踪做的是信念修正（belief revision）：新证据覆盖旧信念。只增不改的日志有"写入"和"检索"，唯独没有"修订"。任务越长，封存错误积累越多，检索就越沦为矛盾之间的谈判。

### 失效模式二：显著性遗忘（Salience-driven forgetting）

这是最深的一种失效模式，因为它源于两个排序函数之间的系统性错配。事件边界按余弦变化幅度选取：保留什么，取决于"**观测看起来变化多大**"。但决定下一步动作是否可行的物理状态——手是否占用、杯子是否稳定、门是否锁上——往往是**视觉上平淡**的变化。

其结果是一种反常的选择压力：缓慢连续的物理变化（逐渐松开的夹爪）不产生感知跳变，于是真正的状态迁移点得不到锚点；而镜头抖动、路人经过产生的视觉突变，反而把真正重要的旧锚点挤出了检索预算。

我们与某前沿系统作者的直接交流，为这种不对称提供了惊人的印证。**训练时**，事件边界由本体信号生成——位姿速度、夹爪状态——然后再做语义标注；**在线执行时**，边界选取却退化为相邻 VLM 块之间的余弦变化——一个纯视觉信号。换句话说，训练管线知道什么在物理上重要（夹爪张开），但部署中的系统只能靠"观测看起来变化多大"去猜。训练-部署之间的这道鸿沟，毫不夸张地说，就是把失效模式二直接烧进了架构里。

> **错配**
>
> 记忆按**显著性**给历史排序；行动按**因果性**向状态索取。两种排序不同，且偏差是系统性的：这个范式在结构上恰恰低估了物理上最关键的信息。

### 失效模式三：重复推导（Repeated re-derivation）

由于状态每一步都从记忆池中重新融合，同一个世界在相邻两步可能检索到不同证据，从而推出不一致的子目标；视觉上相似的历史片段互相串扰；而在最需要历史的新颖场景里，学到的门控机制反而可能把记忆整个关掉。

状态变量的价值恰恰在于持久性：它承诺"**除非出现反证，否则事实不变**"。检索机制给不出这个承诺。每一步的世界都是一次全新的重建，而同一个世界的多次重建之间并不保证一致。

### 失效模式四：误差固化（Error entrenchment）

逐秒规划意味着每个子目标都依赖于那个脆弱的重建状态。推错一步 → 执行错误动作 → 世界被**真实地**改成与错误状态一致的样子 → 后续检索面对的是一个"错误已被坐实"的世界。错误不只是传播，而是被物理世界**固化**。

这正是具身智能与语言建模分道扬镳的地方。在 LLM 里，一个错误 token 污染的是上下文，不是世界；在机器人里，一个错误动作污染的是世界，而被污染的世界又会作为观测反馈回来。于是失效模式一（封存错误）的代价被成倍放大：错误不仅被记住了，还可能已经被**真实地执行**了。

### 本批评不适用的地方

公允需要边界。短期物理动态并不依赖这套记忆机制——实时观测流处理接触变化与物体运动，脆弱的只是长程任务状态。冗余设计（全量压缩历史始终在检索池中）缓解了单点失效。在相关论文的评测分布内，这套机制确实是好用的。

但请注意那个被声称要解决的场景的形状：**长时程、阶段间视觉自相似**。这恰好是该机制在结构上最弱的场景。**范式宣称的目标，正是范式自己的盲区。**

> **笔记本 vs 白板**
>
> 记忆是笔记本：只往后写，靠翻阅找回事实；写错的那页永远在，翻到哪页取决于检索。状态是白板：当前世界是什么就写什么，旧的擦掉。笔记本记得再好，也不是白板——**记忆按"什么值得翻"组织信息，而物理状态必须回答"什么现在为真"。**

---

## 四、物理世界状态框架

批评到此为止，转向提案：一个把状态当作一等变量来维护的系统，应该长什么样？

### 4.1 已被广泛认可的三条性质

文献指向三条要求，我们认可它们，但论证其并不充分：

1. **被持续维护，而非每次重建。** 状态是持久变量，承诺一致性；记忆是它的**输入**，不是它的**载体**。
2. **可被观测修正。** 新证据可以覆盖旧状态——"以为放稳了"可以被"看到倒了"取代——而不是让两条矛盾记录永远竞争。
3. **被物理约束校验。** 检索到的状态要与物理一致性检查对表：手不可能既占用又空闲，杯子不可能既被握持又在桌上。

满足这三条的系统，学习目标也随之改变。不再是：

$$\text{指令} \longrightarrow \text{动作}$$

而是：

$$\text{当前状态} \longrightarrow \text{目标状态} \longrightarrow \text{状态迁移} \longrightarrow \text{电机技能}$$

动作只是改变世界的工具；真正的智能，是理解状态如何演化。

### 4.2 第四条、也最贵的性质：反事实可模拟性

我们认为，上述三条必要但不充分。一个只记录"现在"的状态无法支撑规划；规划要求回答"**如果我做 A，世界会变成什么样？做 B 呢？**"这正是世界模型区别于状态追踪器的地方。

> **性质四：反事实可模拟性**
>
> 物理世界状态必须支持**推演（rollout）**：给定当前状态 $s$ 和候选动作 $a$，系统必须能模拟后继状态 $s'$ 的分布。一个不能向前推演的状态，是数据库，不是世界模型。

第四条性质代价最高，它迫使我们去澄清：世界模型究竟**是干什么的**。

### 4.3 从性质到具体草图

只有性质而没有草图，性质就还是口号。因此我们给出一个最小化的、刻意保守的具体形态——不是最终设计，而是一个"存在性证明"：PWS 用今天的技术就能实现。

**状态表示：带关系谓词的物体槽位。** 状态 $s$ 由一组**物体槽位**（每个槽位携带带类型的属性）和一组槽位间的**关系谓词**构成。以"杯子与门"为例：

| 槽位 / 谓词 | 取值域 |
|---|---|
| `hand.status` | {`free`, `holding(x)`} |
| `cup.support` | {`table`, `hand`, `air`, `shelf`} |
| `cup.stable` | {`true`, `false`, `unknown`} |
| `door.latch` | {`latched`, `unlatched`} |
| `on(cup, table)` | {`true`, `false`} |

两个设计选择很关键。其一，属性有**封闭取值域**，于是一次"修订"就是一次赋值，而不是自由编辑。其二，`unknown`（未知）是一等公民：追踪器被允许说"我不知道杯子稳不稳"——这是均值池化的记忆无法表达的。持久是默认：槽位保持其值，直到证据迫使它改变。

**修订机制：证据 → 提案 → 校验 → 提交。** 观测不直接写入状态。每个新观测先产生**证据**，证据被转成**提案**（一组槽位更新），提案穿过物理一致性层，只有通过校验的提案才被**提交**。未通过的提案被拒绝并记录——这本身又成为新证据（"我上一个动作没有达到预期效果"），触发恢复流程，而不是让状态被静默污染。

```
state  = init_slots()                    # 跨步骤持久存在
memory = []                              # 只增不改，仅作输入
for t in 每一步:
    obs  = perceive()
    memory.append(obs)
    ev   = extract_evidence(obs, state)       # 如"杯子倾斜40度"
    prop = propose_updates(ev, state)         # 如 cup.stable := false
    ok   = consistency_check(prop, state)     # 硬性物理规则
    if ok:
        state.commit(prop)                    # 修订：覆盖旧值
    else:
        state.flag_conflict(prop); recover()  # 绝不保留矛盾
    goal = task_index(state, instruction)     # 查询，而非重新推导
    a    = plan(state, goal, world_model)     # 使用 P(s'|s,a) 推演
    execute(a)
```

这个循环有三个要点。（i）**记忆是输入，不是载体**：笔记本可以无限变厚，白板不会。（ii）**修订是覆盖，不是追加**：失效模式一被构造性地消除。（iii）**目标变更是重新查询**：`task_index` 把（可能已变更的）指令映射到**同一组**持久槽位上，于是任务中途换目标时，有效的物体状态得以保留（见 4.5 节）。

**一致性层：硬的、不可微分的、可独立测试的。** 这一层编码诸如**互斥性**（`hand.status=free` 排除 `holding(x)`）、**支撑传递性**（若 `cup.support=hand` 则 `hand.status=holding(cup)`）、**动作前置条件**（`open(door)` 要求 `hand.status=free` 且 `door.latch=unlatched`）等不变量。这些是经典的、可手写的规则——这是刻意的：它们不需要学习，可以单独做单元测试，为学到的软约束（世界模型）提供它无法自给的硬地基。

**与模拟器的耦合：转移核是借来的，不是重建的。** 对于推演性质（4.2 节），世界模型不需要从像素重新学习物理。候选状态迁移的打分，可以把校验过的状态耦合到现有高保真模拟器（物理引擎负责近期、几何层面的可行性）与学习型视频/潜空间世界模型（负责表观层面、长时程部分）的组合。追踪器持有 $s$；模拟器与学习模型共同近似 $P(s'|s,a)$；一致性层审计两者。每个组件都可替换——这种模块化是特性，不是妥协。

### 4.4 重新定位世界模型：从状态到状态转移核

我们认为，当前的混乱在于：前沿系统让世界模型同时干两份工——既表示当前状态，又预测状态演化——并且第一份工干得很差，因为它把状态当作对记忆的推断来处理。我们的提案是把两份工拆开：

- **状态追踪器（state tracker）持有当前状态 $s$ 的值。** 它是持久的、可修正的、受物理约束的（性质一至三）。它由观测更新，被新证据修正。用经典 AI 的语言说，它是一个信念状态（belief state）——但带有一层硬的物理一致性检查。
- **世界模型持有状态转移核 $P(s'|s,a)$。** 它不为"现在什么是真的"下断言；它预测"如果做了某事，什么**会**变成真的"。它的正当角色不是**成为**状态，而是为一个别处持有的状态**评估未来**。

> **解耦**
>
> 世界模型应当在一个独立维护的状态之上建模**状态转移核**。当前状态 $s$ 的值属于一个满足性质一至三的状态追踪器；世界模型提供 $P(s'|s,a)$，用于规划与约束校验。把两者混为一谈——让转移核的不确定性冒充状态的确定性——正是四种失效模式背后的架构性错误。

这个解耦也重新框定了安全讨论。在安全关键场景中，危险很少来自"世界模型模拟得不准"，而来自**当前状态本身就是错的**（失效模式一）。一个完全精确的转移核，从一个被污染的 $s$ 出发向前推演，产出的是自信而错误的未来。解耦使 $s$ 的完整性成为一个可分离、可审计的问题——它可以接受独立的硬性物理一致性检查，而不必裹在世界模型学到的软约束里。

### 4.5 已经可见的汇合

PWS 方向并非无人区。物体可寻址（object-addressable）方法已经在维护持久的、可寻址的物体槽位，并联合预测以物体为中心的世界状态与动作——这是物体层 PWS 的清晰雏形，只是没有任务语义。与此同时，事件记忆类 WAM 把历史压缩为任务层的进度——这是任务层的雏形，只是（如我们所论证的）一个名不副实的"状态"。

> **两条压缩线，及其必然的汇合**
>
> - **任务层**：历史 → 任务进度（但目前是无序平均）
> - **物体层**：历史 → 物体状态（但目前没有任务索引）
>
> 下一步是合并两条线：**进度由物体状态定义，物体状态由任务目标索引。**

这一汇合直接回答了我们向某前沿 WAM 系统作者提出的一个问题：**当用户在任务中途变更目标时，由此前目标推断出的"进度"，是从头重估，还是增量修正？** 作者的回答是工程层面的，且颇具启发性：系统在每个动作块前检测 prompt 是否变化，只对变化的模态重新编码，并在下一块重新注入。请注意这个回答覆盖了什么、没覆盖什么。它处理的是**条件变化**——新的一句话、新的一张目标图——而不是**进度重估**：机制中没有任何东西会基于新目标重新审视"已经完成了什么"。在记忆框架内这不足为奇：进度没有锚定在物理状态上，目标一换，那个平均出来的"进度"就悬空了，唯一可用的动作就是重新编码条件、然后信任下一次重建。而在 PWS 框架里，答案是结构性的：物体状态在目标变更后依然成立（杯子还是在桌上），需要修订的只是任务层的索引（新目标关心哪些物体状态）。**目标变更变成了换查询，而不是状态崩溃。**

---

## 五、讨论：产业含义

PWS 视角澄清了当前具身智能版图中几个令人困惑的现象。

### 5.1 为什么结构化部署能成功

迄今为止最可信的商业部署——汽车零部件搬运、电池产线物流、仓库周转箱运输——有一个隐秘的共性：**环境被结构化到把状态空间坍缩了**。工装、夹具、传送带、固定工位，充当了一块外置的物理世界状态：杯子的位置由夹具保证，而不需要模型去追踪。用我们的术语说，这些部署成功，是因为它们把 PWS **外包给了环境**——绕开了那个缺失的变量，而不是解决了它。

这解释了一个看似悖论的模式：榜单分数平平的公司实现了数月级别的工厂部署，而拥有前沿世界模型能力的公司仍停留在实验室 demo。前者选择了不需要 PWS 的环境；后者尝试的则是 PWS 无法回避的领域（家庭、非结构化操作）。

### 5.2 为什么世界模型原生公司面临结构性困境

一类公司正在兴起，其核心命题是：世界模型本身可以作为机器人的预训练基础模型，以极少量上下文数据绑定到多样本体。我们的分析表明，这一命题内含张力：它所宣称的优势（数据效率、跨本体泛化）预设了世界模型内部维护着一个足以支撑少样本落地的状态表示。而如果底层的状态机制是均值池化加记忆检索，这个命题就没有架构地基。

这并不意味着该命题错误；它意味着该命题的成立取决于 PWS 问题是否被解决——要么把 PWS 内化进世界模型，要么如我们所提议的那样显式解耦状态追踪。在我们看来，这类公司中把这个问题当作工程细节而非架构鸿沟的，都低估了它的难度。

### 5.3 评测的缺口

当前的具身世界模型基准测量的是成功率和物理似然性，而不是**失效模式的分布**。99% 的成功率掩盖了那 1% 的失败究竟是"杯子放得稍偏"还是"把热水泼到了人身上"。对家庭和高危场景而言，这个区别就是一切。此外，对抗性评测——故意制造物理矛盾以探查状态追踪的完整性——几乎不存在，部分原因是物理红队测试有真实成本。

可解释性维度让问题雪上加霜。某前沿系统的作者确认，子目标仍是隐式的：控制使用的是规划 token 的隐状态，离散 token 虽可用于调试，但"不能视为完全可靠的自然语言解释"。一个不可解释的子目标表示不只是调试上的不便，它是任何形式化验证或对抗性安全审计的屏障。**你无法对一个读不出来的子目标做红队测试。**

我们认为，面向 PWS 的评测（测试可修正性、矛盾下的一致性、目标变更的鲁棒性）是任何非结构化场景部署声明可信的前提。

### 5.4 与 LLM 类比的关系

把具身世界模型类比为基于 LLM 的智能体操作系统，是时髦的，这个类比也有启发性：两者都包含一个学到的"内部世界"和一个轻量执行层（工具调用 vs 电机指令）。但这个类比恰恰在最关键的地方断裂。软件智能体的工具接口是离散的、有规约的；物理智能体的动作是连续的、受动力学约束的，而且错误会改变世界本身（失效模式四）。"工具调用失败"和"抓取失败"的成本结构根本不同。这就是为什么我们认为，具身智能中的状态问题不是 LLM 记忆问题的重演，而是一个更难的、与物理纠缠在一起的变体。

### 5.5 本立场的局限

我们也指出自身论证的三个局限。第一，PWS 框架是一个立场，不是一个已实现的系统；以实时速率维护一个持久的、可修正的、物理一致的、可反事实模拟的状态，其工程成本未知，也可能高得难以承受。第二，混合架构可能模糊我们划定的边界——一个结构足够好的学习型记忆，也许能在没有显式状态变量的情况下逼近 PWS 的行为；这种逼近是否足够，是一个开放的实证问题。第三，我们的产业分析基于公开信息和匿名化案例；对特定系统内部已在多大程度上纳入了类 PWS 机制，我们可能低估，也可能高估。

---

## 六、研究议程

我们按难度递增给出一个具体议程。

1. **面向 PWS 的评测。** 扩展具身世界模型基准，加入状态可修正性测试（新证据是否覆盖旧信念？）、矛盾一致性测试（系统能否检测物理上不可能的状态？）、目标变更鲁棒性测试（变更目标是否保住有效的物体层状态、同时更新任务层索引？）。
2. **作为插件的显式状态追踪器。** 开发可挂接到现有 WAM 系统上的状态追踪模块，持有当前状态 $s$ 的值，让世界模型只提供 $P(s'|s,a)$。最直接的交付物是一项消融实验：显式状态追踪器能否在长时程基准上降低失效模式一至四？
3. **物理一致性层。** 形式化一个约束校验层（手不可能既占用又空闲；杯子不可能既被握持又被支撑），在状态被提交进追踪器之前先行审计。这一层应当是硬的、不可微分的、可独立测试的。
4. **任务-物体状态融合。** 实现第四节指出的汇合：物体层状态由任务目标索引，任务进度由物体状态**定义**而非从历史平均而来。其结果应当是一个能在目标变更下结构性存活、而非启发式幸存的状态表示。
5. **带完整性保证的反事实推演。** 最难的问题：从一个完整性由独立追踪器保证的状态出发，经由一个诚实报告自身不确定性的转移核向前推演。在我们看来，这一问题的成功解决，才构成第一个真正配得上"具身世界模型"之名的架构。

---

## 七、结论

回到开篇那个瞬间：杯子放下，机器人转身走向门。

记忆回答"发生过什么"。任务描述回答"为什么做"。但真正驱动机器人下一步动作的，是一个持续更新、始终存在、且与物理世界自洽的"**现在什么是真的**"的表示——一个物理世界状态。

世界动作模型的演化轨迹，在我们看来已经清晰：从历史（History），到任务进度状态（Task Progress State），再到物理世界状态（Physical World State）。第一跳——压缩历史——已被证明；第二跳——物体层的持久状态——已有原型；第三跳是汇合：**进度由物体状态定义，物体状态由任务目标索引**——全部由追踪器持有：持久、可修正、经一致性校验；而世界模型做只有它能做的事——评估未来。

机器人不生活在时间轴上。它们生活在一个不断演化的物理世界中。记忆捕捉不到这一点。状态可以。

---

## 参考文献

完整参考文献（54 条，含世界模型基础、WAM 与事件记忆、物体中心表征、VLA、信念状态与任务运动规划、基准评测、支撑技术、私有通信八类）请见同仓库英文版论文 PDF。中文译版保持与英文版一致的引用编号。
